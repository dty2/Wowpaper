> [!IMPORTANT]
> 在工作目录中会有pid, sock和log文件
> 因此, 守护进程必须与 CLI 使用相同的工作目录
> 否则可能会导致错误和奇怪的问题
> 所以除非绝对必要, 请不要手动设置"--work-dir"

默认情况下, 壁纸列表存放在配置目录`$HOME/.config/wow`

(当然, 你也可以放在其他位置, 只需在 config.toml 中进行配置即可)
